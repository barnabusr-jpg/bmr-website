name: Execute E2E Endpoint Validation

on:
  workflow_dispatch:

jobs:
  run-validation-test:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Live Production API Endpoint
        run: |
          curl -X POST https://lab.bmradvisory.co/api/dispatch-directives \
            -H "Content-Type: application/json" \
            -d '{
              "groupId": "8e75a768-027e-4aa0-ab1b-6c574ccbd38c",
              "orgName": "SYSTEM TEST",
              "parentAuditId": "8e75a768-027e-4aa0-ab1b-6c574ccbd38c",
              "emails": {
                "exec": "barnabusr@gmail.com",
                "mgr": "hello@bmradvisory.co",
                "tech": "barnabusr@outlook.com"
              }
            }'
